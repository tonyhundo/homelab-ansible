---
- name: Package Installation
  hosts: rundeck
  become: true
  tasks:
    - name: Install openjdk-25
      ansible.builtin.apt:
        name:
          - openjdk-25-jre-headless
        state: latest
